# Juice Shop Baseline — AFTER (example)

- **Commit hash:** `abc1234` (record actual)
- **Juice Shop version:** e.g. `15.x` (check actual `package.json`)
- **Node version:** `20.x` (match `.nvmrc`)
- **Package manager:** `npm`
- **`npm install` result:** ✅ succeeded (peer dep warnings expected, not errors)
- **`npm start` result:** ✅ listening on `:3000`, DB seeded, no startup errors
- **App reachable at `http://localhost:3000`:** ✅
- **Score-board reachable at `/#/score-board`:** ✅
- **Challenge count shown on score-board:** ~100+ (record actual — varies by version)
- **Manual smoke test:** ✅ registered a test user, logged in, viewed product, added to basket
- **`CLAUDE.md` from `/init` reviewed and adjusted:** ✅
- **Preamble stating authorized training added to `CLAUDE.md`:** ✅
